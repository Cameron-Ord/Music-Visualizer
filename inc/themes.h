#ifndef THEMES_H
#define THEMES_H
#include <SDL2/SDL_render.h>

struct NordTheme {
  SDL_Color prim;
  SDL_Color secondary;
  SDL_Color tertiary;
};

struct DraculaTheme {
  SDL_Color prim;
  SDL_Color secondary;
  SDL_Color tertiary;
};

#endif
